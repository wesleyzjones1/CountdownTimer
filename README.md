# Countdown Timer Widget

This project is a lightweight Electron desktop widget built from the original React countdown timer.

## Launch

Install dependencies once:

```bash
npm install
```

Start the widget with one command:

```bash
npm run widget
```

The widget launches as a frameless window that stays above other windows.
